name: Update Lean Project

on:
  schedule:
    - cron: "0 0 * * *" # every day at midnight UTC
  workflow_dispatch: # allows workflow to be triggered manually

jobs:
  update_lean:
    permissions:
      contents: write
      issues: write

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false
          python-version: "3.13"

      - name: Record starting HEAD
        id: before
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Select next Lean release
        env:
          GH_TOKEN: ${{ github.token }}
        run: uv run python .github/scripts/select-next-toolchain.py

      - name: Update Lean project
        id: lean-update
        uses: leanprover-community/lean-update@5010048b67406bb7481dee769a4dcd1f7e560393
        with:
          update_lean_toolchain: never
          update_if_modified: lean-toolchain
          on_update_succeeds: commit
          on_update_fails: issue

      - name: Tag updated toolchain commit
        if: github.ref == 'refs/heads/main' && steps.lean-update.outputs.result == 'update-success'
        env:
          BEFORE_SHA: ${{ steps.before.outputs.sha }}
        run: bash .github/scripts/tag-toolchain.sh

      - name: Fail when the toolchain update fails validation
        if: steps.lean-update.outputs.result == 'update-fail'
        run: |
          echo "The next Lean release failed validation." >&2
          exit 1
